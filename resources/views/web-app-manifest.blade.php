{
    "name": "{{ config('brand.name') }}",
    "short_name": "{{ config('brand.name') }}",
    "description": "{{ config('brand.meta.description') }}",
    "start_url": "/",
    "display": "standalone",
    "background_color": "{{ config('brand.colors.light') }}",
    "theme_color": "{{ config('brand.colors.primary') }}",
    "icons": []
}
